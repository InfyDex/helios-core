version: "2"
sql:
  - engine: "postgresql"
    queries: "sql/queries/"
    schema: "migrations/001_init.sql"
    gen:
      go:
        package: db
        out: "pkg/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_empty_slices: true
